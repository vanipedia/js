if (wgAction == 'view' && wgCanonicalNamespace === '') {
    $(document).ready(function() {
        $('p > b').each(function() {
            $this = $(this);
            if ($this.children().size() == 0) {
               $this.css('color', '#296F39');
             }
        })    
    });
}